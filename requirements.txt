# RLPE requirements
# Python >= 3.11

# -------------------------
# Core pipeline dependencies
# -------------------------
requests>=2.31.0
numpy>=1.26.0
opencv-python>=4.9.0
pymupdf>=1.24.0
pandas>=2.2.0
tqdm>=4.66.0
scikit-image>=0.22.0
pillow>=10.0.0

# -------------------------
# OCR / Taxonomy
# -------------------------
paddleocr>=2.7.0
easyocr>=1.7.1
taxonerd>=1.5.3
spacy>=3.7.0

# -------------------------
# LLM / Gemma backends
# -------------------------
transformers>=4.45.0
accelerate>=0.33.0
bitsandbytes>=0.43.0

# -------------------------
# MiniMax M3 API (Anthropic-compatible)
# -------------------------
anthropic>=0.40.0,<0.50
python-dotenv>=1.0.0

# -------------------------
# API / Queue / Service
# -------------------------
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.8.0
celery>=5.4.0
redis>=5.0.0

# -------------------------
# Training utilities
# -------------------------
datasets>=2.19.0
scikit-learn>=1.5.0
scipy>=1.13.0

# -------------------------
# YOLO figure detection (optional)
# Brings ~150 MB of torch + ultralytics dependencies.
# Enable via Settings tab or --use-yolo-figures flag.
# -------------------------
ultralytics>=8.1.0

# -------------------------
# -------------------------
# OpenDataLoader (alternative PDF parsing, requires Java 11+)
# -------------------------
# opendataloader-pdf>=0.0.0

# -------------------------
# Published JSON Schema (contract)
# -------------------------
# Note: pydantic is already declared above (>=2.8.0). The schema
# extra (pydantic>=2.5) was redundant -- pip's resolver takes the
# highest declared version, so this section now only pulls in
# jsonschema. Keeping a single source of truth for the pydantic
# version avoids drift between the API section and the schema
# section.
jsonschema>=4.21

# -------------------------
# Notes
# -------------------------
# 1) SAM2 should be installed from source repository (editable install).
# 2) If you only need llama.cpp / Ollama inference, transformers stack can be omitted.
# 3) torch installation should follow your CUDA environment (recommended via conda).
# 4) opendataloader-pdf requires Java 11+. Install with: pip install opendataloader-pdf
#    Enable in pipeline via extra config: "use_opendataloader": True
